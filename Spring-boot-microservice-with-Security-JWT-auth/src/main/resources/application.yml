spring:
  application:
    name: Spring-boot-microservice-with-Security-JWT-auth
  datasource:
    url: jdbc:mysql://localhost:3306/arvind
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 9090